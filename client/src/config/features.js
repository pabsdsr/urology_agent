/** Set to true to restore the clinical assistant chat UI and nav link. */
export const CHAT_ENABLED = false;

/** Landing route after login when chat is disabled. */
export const DEFAULT_AUTHENTICATED_ROUTE = "/schedule";
